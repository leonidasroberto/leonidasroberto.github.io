/*
window.onload = () => {
    const titles = ["Code", "Plan", "Monitor", "Operate", "Deploy", "Release", "Test", "Build"]
    var count = 0
    setInterval(()=>{ 
        if (count > titles.length-1){
            count = 0;
        }
        document.title = titles[count]
        count++
        
    },1500);
}
*/